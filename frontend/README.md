# Frontend

React + TypeScript + Vite frontend for WSB Analyst.

## Commands

```bash
npm install
npm run dev
npm run build
```

The app expects the FastAPI backend at `VITE_API_BASE_URL` or `http://127.0.0.1:8000` by default.
